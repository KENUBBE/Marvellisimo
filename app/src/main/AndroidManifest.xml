<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.marvellisimo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">

        <activity android:name=".favorite.FavSerieList"
                  android:launchMode="singleTop">
        </activity>
        <activity android:name=".favorite.FavCharList"
                  android:launchMode="singleTop">
        </activity>
        <activity android:name=".favorite.FavoriteActivity"
                  android:launchMode="singleTop">
        </activity>
        <activity android:name=".serie.SerieInfoActivity"
                android:launchMode="singleTop">
        </activity>
        <activity android:name=".character.CharInfoActivity"
                  android:launchMode="singleTop">
        </activity>
        <activity android:name=".serie.SerieActivity"
                  android:launchMode="singleTop">
        </activity>
        <activity android:name=".character.CharacterActivity"
                  android:launchMode="singleTop">
        </activity>
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>